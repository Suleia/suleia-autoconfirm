# Functional Parity Engine

Parity compares individual dimensions and never emits an opaque global percentage. Each dimension records expected and actual values, masked evidence, difference reason, severity, migration blocker and remediation.

Results are MATCHED, PARTIAL, DIVERGENT, BLOCKED, NOT_COMPARABLE and NOT_ASSESSED. Identity ambiguity makes a dimension not comparable; a critical divergence remains visible even when every other dimension matches.

